import React from 'react';
import { AbsoluteFill } from 'remotion';

/**
 * Scene 1: Dot Bounce (frames 0-120)
 * Pure visual context — the dot is rendered by the orchestrator.
 */
export const SceneDotBounce: React.FC = () => {
  return <AbsoluteFill />;
};
